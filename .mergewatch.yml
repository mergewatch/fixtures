# Keep the suite on Sonnet 4.5 (fixtures#585). This overlay REPLACES the
# repo-root .mergewatch.yml, so without repeating the pin here the fixture
# falls back to the deploy default (Opus 4.6) whose daily token quota the
# suite exhausts — those fixtures then dead-letter while the rest pass.
model: us.anthropic.claude-sonnet-4-5-20250929-v1:0

# The legacy CSV importer is frozen until the new importer replaces it: it is
# edited only to keep it compiling, and review comments on it are never acted
# on. This file is listed too — it changes only when these lists change, and a
# review of the list itself tells us nothing.
skipPatterns:
  - "src/legacy/**"
  - ".mergewatch.yml"
