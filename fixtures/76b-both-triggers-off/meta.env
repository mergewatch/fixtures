BRANCH=fixture/76b-both-triggers-off
TITLE=feat: add queue drain worker
TAGS=config,triggers
MODE=pr
BODY=Adds a worker that drains the job queue and posts each job to the jobs API. Also pauses automated reviews on this repo for now.
DRAFT=false
POST_OPEN_HINT=Confirm the PR is silent on open, then comment '@mergewatch review' and confirm it stays silent.
