BRANCH=fixture/76b-both-triggers-off
TITLE=E2E-76b: Skip — autoReview and reviewOnMention both false (no trigger path)
BODY=Verifies that with rules.autoReview:false and rules.reviewOnMention:false no path triggers a review — neither opening the PR nor '@mergewatch review' — and that each attempt records its own distinct skip reason. Pair with 76a-review-on-mention-off. See fixtures/76b-both-triggers-off/README.md
DRAFT=false
POST_OPEN_HINT=Confirm the PR is silent on open, then comment '@mergewatch review' and confirm it stays silent.
