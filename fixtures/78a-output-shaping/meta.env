BRANCH=fixture/78a-output-shaping
TITLE=E2E-78a: Output shaping — minSeverity and maxFindings
BODY=Verifies minSeverity filters by tier inclusively and maxFindings truncates by RANK (keeping the most severe, not the first found) with the truncation disclosed rather than silent, using a diff that reliably produces info, warning and critical findings. Pair with 78b-post-summary-on-clean. See fixtures/78a-output-shaping/README.md
DRAFT=false
POST_OPEN_HINT=Walk the minSeverity ladder by editing .mergewatch.yml on the branch (info → warning → critical) and pushing between each step.
