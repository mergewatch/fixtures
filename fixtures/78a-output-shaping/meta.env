BRANCH=fixture/78a-output-shaping
TITLE=feat: add profile sync and session lookup helpers
TAGS=config,output,correctness
MODE=pr
BODY=Adds label formatting, batch profile sync, and a session lookup against the sessions table. Wires in the shared pg pool for the new queries.
DRAFT=false
POST_OPEN_HINT=Walk the minSeverity ladder by editing .mergewatch.yml on the branch (info → warning → critical) and pushing between each step.
