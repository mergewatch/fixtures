{
  "_source": "README Step 2 precondition — E2E-39, SETUP HALF ONLY. FB-C maps 👎/🤔 to disputeCount and 👍/❤️/🚀 to agreementCount on the FindingDispositionRecord — every stated outcome is a DynamoDB counter behind a human reaction, and the self-reaction exclusion needs the bot to react. Asserted here: the inline finding comment the human reacts TO actually exists. All three share one bait file — src/admin-endpoint.ts, an unauthenticated admin endpoint the README calls out as reliably drawing an inline-comment-eligible Critical — so the setup half is deterministic in the same way E2E-03 and E2E-28b are. NEEDS_HUMAN_STEP means automated setup, manual completion: the setup half is gradeable and was costing a real PR and a real review while asserting nothing (fixtures#1076).",
  "comment": "present",
  "findings": {
    "critical": {
      "min": 1
    }
  },
  "inlineComments": {
    "min": 1
  }
}
