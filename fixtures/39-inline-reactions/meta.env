BRANCH=fixture/39-inline-reactions
TITLE=feat: add users listing endpoint for the admin dashboard
TAGS=inline,fp,correctness
MODE=pr
BODY=Adds a GET route that returns the full user list for the admin dashboard. Wiring the table UI to this in a follow-up.
DRAFT=false
POST_OPEN_HINT=After the first review renders an inline-thread Critical, add reactions on the inline bot comment via the GitHub UI (👎 then 🚀, then remove the 👎) and inspect the FindingDispositionRecord counters.
# Opens its PR normally, then needs a person (or a local agent) to finish:
# see POST_OPEN_HINT above. Excluded from --automated because an
# unattended run cannot perform that step — it is NOT MANUAL_ONLY, which
# would stop the PR being created at all and leave nothing to act on.
NEEDS_HUMAN_STEP=true
