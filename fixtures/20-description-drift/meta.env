BRANCH=fixture/20-description-drift
TITLE=feat: persist user preferences across reloads
TAGS=agents,output
MODE=pr
BODY=This PR adds preference persistence using `localStorage.setItem` so user choices survive page reloads. The key format is `pref:<name>`.
DRAFT=false
