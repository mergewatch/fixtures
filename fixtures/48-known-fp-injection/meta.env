MANUAL_ONLY=true
TITLE=E2E-48: FB-L — `{{KNOWN_FP_PATTERNS}}` prompt injection (TARGET, opt-in)
BODY=Pre-seed a high-dispute cluster, set feedback.learnFromDisputes:true in .mergewatch.yml, then open a PR matching the cluster's sigTokens. NOT YET IMPLEMENTED on the bot side — this is the TARGET fixture.
