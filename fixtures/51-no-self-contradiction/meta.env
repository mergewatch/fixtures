BRANCH=fixture/51-no-self-contradiction
TITLE=E2E-51: FP-J L2 — verifier honours prior recommendations
BODY=Verifies round-2 re-review does NOT critique the application of a prior recommendation (the verifier respects its own prior advice). Two-step — see fixtures/51-no-self-contradiction/README.md
DRAFT=false
POST_OPEN_HINT=After the first review surfaces a recommendation (e.g. 'add try/catch around the fetch'), apply the exact fix the bot suggested and push. Round-2 must NOT critique the application of that fix.
