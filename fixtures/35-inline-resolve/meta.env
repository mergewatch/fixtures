BRANCH=fixture/35-inline-resolve
TITLE=E2E-35: FP-F — inline-reply resolve memory
BODY=Verifies that an inline `/resolve` reply persists the finding's stable key so the next review doesn't re-emit it. Two-step — see fixtures/35-inline-resolve/README.md
DRAFT=false
POST_OPEN_HINT=After the first review lands and renders an inline thread on the Critical, reply 'resolved' (or '/resolve') in that thread as the PR author. Then push a no-op commit (git commit --allow-empty -m 'sync' && git push) to trigger re-review. See fixtures/35-inline-resolve/README.md.
