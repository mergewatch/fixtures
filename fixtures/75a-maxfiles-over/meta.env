BRANCH=fixture/75a-maxfiles-over
TITLE=E2E-75a: Skip — maxFiles ceiling exceeded (visible skip)
BODY=Verifies a PR over rules.maxFiles is skipped with a VISIBLE 'Review skipped' check run naming the limit — unlike autoReviewOff, this skip is surfaced, not silent — and that '@mergewatch review' overrides it. Pair with 75b-maxfiles-boundary. See fixtures/75a-maxfiles-over/README.md
DRAFT=false
POST_OPEN_HINT=Comment '@mergewatch review' on the PR to confirm the mention overrides the maxFiles skip.
