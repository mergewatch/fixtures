BRANCH=fixture/75a-maxfiles-over
TITLE=feat: add string and array utility helpers
TAGS=config,skip
MODE=pr
BODY=Adds a few small helpers (toSlug, clamp, unique, chunk) and tightens the review config to keep automated reviews on small PRs.
DRAFT=false
POST_OPEN_HINT=Comment '@mergewatch review' on the PR to confirm the mention overrides the maxFiles skip.
