MANUAL_ONLY=true
TITLE=E2E-47: FB-K — Suggest `.mergewatch.yml` rule CTA
TAGS=fp,output
MODE=pr
BODY=Inspect the "Suggest ignore rule" CTA on a high-dispute-rate cluster row in the themes table. Requires pre-seeded data: cluster with disputeRate > 80% AND surfaceCount >= 5.
