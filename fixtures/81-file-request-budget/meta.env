BRANCH=fixture/81-file-request-budget
TITLE=feat: add greet and summarize helpers using shared math utils
TAGS=config,agents,context
MODE=pr
BODY=Adds a greeting helper and a summarize function that totals and scales counts via the shared add/multiply utils. Also enables bounded codebase-aware review so cross-file context is available.
DRAFT=false
POST_OPEN_HINT=Re-run with codebaseAwareness:false and with maxContextKB set very low, pushing between each, and compare how the finding describes src/utils.ts.
