BRANCH=fixture/81-file-request-budget
TITLE=E2E-81: Codebase awareness — file-request budget
BODY=Verifies that with codebaseAwareness:true agents fetch an out-of-diff file the change depends on and reflect its real contents, that maxFileRequestRounds and maxContextKB are both enforced, that hitting the budget degrades to a partial-context note rather than failing, and that with awareness off no agent invents the unfetched file's contents. See fixtures/81-file-request-budget/README.md
DRAFT=false
POST_OPEN_HINT=Re-run with codebaseAwareness:false and with maxContextKB set very low, pushing between each, and compare how the finding describes src/utils.ts.
