BRANCH=fixture/38-quiet-drop
TITLE=E2E-38: FB-B — quiet-drop derived counter
BODY=Verifies that when the orchestrator silently drops a previously-emitted finding (cited code unchanged), silentDropCount on the FindingDispositionRecord increments. Two-step — see fixtures/38-quiet-drop/README.md
DRAFT=false
POST_OPEN_HINT=After the first review lands, push a small change to an UNRELATED file (don't touch the cited code). Inspect the FindingDispositionRecord for the original finding — if review #2 dropped it, silentDropCount should be 1.
