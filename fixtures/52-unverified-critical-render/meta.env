BRANCH=fixture/52-unverified-critical-render
TITLE=feat: add user lookup by id via the store layer
TAGS=agents,output,verification,correctness
MODE=pr
BODY=Adds lookupUser so callers can resolve a user record through the store layer instead of hitting the DB client directly.
DRAFT=false
