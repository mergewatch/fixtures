MANUAL_ONLY=true
TITLE=E2E-56: TTM — cycle-time rollup (time-to-merge, stage 2)
TAGS=ttm,rollup
MODE=dynamo
BODY=Pre-seed ~15 PRLifecycleRecord rows (reviewed-merged, unreviewed-merged, closed-unmerged, open) with merge spans across hours/days, then trigger the hourly rollup and inspect the cycleTime block on each window's InstallationFPInsight. No fixture PR.
