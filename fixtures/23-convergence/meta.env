BRANCH=fixture/23-convergence
TITLE=feat: add resilient remote config loader
TAGS=agents,verification,correctness
MODE=pr
BODY=Adds tryLoad, a best-effort remote fetch that returns null on failure so callers can fall back to local defaults.
DRAFT=false
POST_OPEN_HINT=After the first review lands, post a '## mergewatch triage' rebuttal then push the logging commit. See fixtures/23-convergence/README.md for step 2.
