BRANCH=fixture/102-skip-patterns
TITLE=chore: stop reviewing the frozen legacy CSV importer
TAGS=config,skip,correctness
MODE=pr
BODY=The legacy CSV importer is frozen — we only touch it to keep it compiling until the new importer lands, and review comments on it are never acted on. Adds src/legacy/** to skipPatterns so those PRs are left alone, along with a small tidy-up in the importer itself.
DRAFT=false
