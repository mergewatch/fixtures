MANUAL_ONLY=true
TITLE=E2E-58: Engagement — /resolve capture (engagement metrics, stage 1)
BODY=On a repo with an active review that surfaced >=1 inline finding, reply /resolve on the inline-finding thread, then inspect the FindingDispositionRecord for a resolveCount increment alongside disputeCount. Manual reply + store inspection, no fixture PR.
