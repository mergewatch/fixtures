BRANCH=fixture/49-re-review-no-anchoring
TITLE=E2E-49: FP-H — anti-anchoring on prior findings
BODY=Verifies round-2 re-review on a fix commit does NOT produce findings that pattern-match against round-1's framing. Two-step — see fixtures/49-re-review-no-anchoring/README.md
DRAFT=false
POST_OPEN_HINT=After the first review surfaces error-handling findings on src/worker.ts, edit src/worker.ts to address them (wrap calls in try/catch with logging), then add a tiny change to src/unrelated.ts that has no error-handling code at all. Push.
