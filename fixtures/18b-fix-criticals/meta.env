PUSH_TO_EXISTING_BRANCH=fixture/18-delta-aware-verdict
TITLE=fix: require admin auth and parameterize the user lookup query
COMMIT_MESSAGE=fix: require admin auth on both handlers and parameterize the user lookup query
BODY=Adds requireAdmin checks to both admin handlers and switches the user lookup to a parameterized query.
