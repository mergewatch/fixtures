BRANCH=fixture/76a-review-on-mention-off
TITLE=feat: add fetchProfile API helper
TAGS=config,triggers,correctness
MODE=pr
BODY=Adds a helper to fetch a user's profile from the API, plus a review-config tweak to stop mention comments from kicking off duplicate runs.
DRAFT=false
POST_OPEN_HINT=After the automatic review lands, comment '@mergewatch review' — no NEW review must appear; check the skip reason is reviewOnMentionOff.
