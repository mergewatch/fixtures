BRANCH=fixture/50-suggestion-redundant
TITLE=feat: add doWork wrapper around the risky task
TAGS=agents,output
MODE=pr
BODY=Small wrapper that runs the task and logs a warning if it throws, so a failure doesn't take down the caller.
DRAFT=false
