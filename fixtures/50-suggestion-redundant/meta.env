BRANCH=fixture/50-suggestion-redundant
TITLE=E2E-50: FP-I — verify suggestion-already-implemented
BODY=Verifies that a finding whose `suggestion` is byte-equivalent to existing code at the cited line is dropped by the verifier with zero LLM cost (Layer 2 short-circuit). See fixtures/50-suggestion-redundant/README.md
DRAFT=false
