BRANCH=claude/fix-greet-bug
TITLE=Fix greeting bug for null and whitespace-only names
BODY=greet() could throw on null input and produced a malformed greeting for whitespace-only names. This change trims the input and falls back to a default greeting in both cases.
DRAFT=false
