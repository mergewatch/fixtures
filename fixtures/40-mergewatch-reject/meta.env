BRANCH=fixture/40-mergewatch-reject
TITLE=feat: expose accounts list for the admin console
TAGS=inline,fp,correctness
MODE=pr
BODY=New GET endpoint returning all users so the admin console can render the accounts table. Pagination can come later once we see real usage.
DRAFT=false
POST_OPEN_HINT=After the first review renders an inline thread, reply '/mergewatch reject style-disagreement we use snake_case here' on the thread. Confirm rejectReasons[] persisted and bot confirmation reply posted.
# Opens its PR normally, then needs a person (or a local agent) to finish:
# see POST_OPEN_HINT above. Excluded from --automated because an
# unattended run cannot perform that step — it is NOT MANUAL_ONLY, which
# would stop the PR being created at all and leave nothing to act on.
NEEDS_HUMAN_STEP=true
