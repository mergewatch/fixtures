BRANCH=fixture/79-ux-block
TITLE=feat: add order lookup, webhook verification, and receipt enqueue
TAGS=config,output,ux
MODE=pr
BODY=Adds an order lookup query, a webhook signature check, and a fire-and-forget receipt enqueue. Also tunes the review comment presentation (tone, header, checklist) in .mergewatch.yml.
DRAFT=false
POST_OPEN_HINT=Re-run with each tone (collaborative → direct → advisory) by editing .mergewatch.yml on the branch, and diff the FINDING SET between runs — it must not change.
