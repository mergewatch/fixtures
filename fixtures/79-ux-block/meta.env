BRANCH=fixture/79-ux-block
TITLE=E2E-79: UX block — comment presentation
BODY=Verifies the ux config block controls only presentation: tone reworks phrasing without changing which findings are reported, showWorkDone / showSuppressedCount / reviewerChecklist / allClearMessage each toggle their own section, and commentHeader is escaped so markdown or HTML in it cannot inject into every review comment. See fixtures/79-ux-block/README.md
DRAFT=false
POST_OPEN_HINT=Re-run with each tone (collaborative → direct → advisory) by editing .mergewatch.yml on the branch, and diff the FINDING SET between runs — it must not change.
