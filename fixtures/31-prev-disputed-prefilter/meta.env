BRANCH=fixture/31-prev-disputed-prefilter
TITLE=E2E-31: FP-B — pre-filter previousFindings by disputedKeys
BODY=Verifies that prior findings whose stable identity key is in disputedKeys (W3 rebutted set) are EXCLUDED from the orchestrator's previousFindings block entirely. Two-step — see fixtures/31-prev-disputed-prefilter/README.md
DRAFT=false
POST_OPEN_HINT=After the first review lands, post a '## mergewatch triage' rebuttal on the data-access finding (as PR author), then push a no-op commit (git commit --allow-empty -m 'sync' && git push). See fixtures/31-prev-disputed-prefilter/README.md.
