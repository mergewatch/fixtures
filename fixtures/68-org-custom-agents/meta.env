BRANCH=fixture/68-org-custom-agents
TITLE=E2E-68: Org Custom Agents (#235)
BODY=Verifies an installation-level custom agent runs in union with the repo's .mergewatch.yml customAgents (org wins on a name clash), that an advisory agent only surfaces findings while a blocking agent's critical forces REQUEST_CHANGES plus a failing 'Blocked by org agent' check, and that scope + path targeting gate execution. Configure the org agent in Settings → Custom Agents BEFORE applying. See fixtures/68-org-custom-agents/README.md
DRAFT=false
POST_OPEN_HINT=Toggle the org agent advisory → blocking in Settings → Custom Agents, then re-review with '@mergewatch review' to compare verdicts.
