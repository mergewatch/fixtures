BRANCH=fixture/80a-conventions-order
TITLE=feat: add status helper and document engineering conventions
BODY=Adds a describeStatus helper and writes up our house rules across AGENTS.md, CONVENTIONS.md, and the docs folder so the review can enforce them. The helper still needs cleanup to match those rules.
DRAFT=false
POST_OPEN_HINT=Delete the winning conventions file on the branch and push to walk down the fallback chain one step at a time.
