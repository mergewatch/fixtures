BRANCH=fixture/17-grounding-hallucinated-anchor
TITLE=feat: persist chat turns to session and message stores
BODY=Adds a persistChat helper that writes each chat turn to the session store and then the message store. The writes are sequential because the message insert needs the session id.
DRAFT=false
