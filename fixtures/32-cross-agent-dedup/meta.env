BRANCH=fixture/32-cross-agent-dedup
TITLE=E2E-32: FP-C — pre-orchestrator cross-agent dedup
BODY=Verifies that when two or more agents flag the same (file, line) with overlapping titles, the duplicates merge BEFORE the orchestrator's LLM call. See fixtures/32-cross-agent-dedup/README.md
DRAFT=false
