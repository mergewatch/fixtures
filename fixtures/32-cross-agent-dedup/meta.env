BRANCH=fixture/32-cross-agent-dedup
TITLE=Add run() command helper
TAGS=agents,dedup,correctness
MODE=pr
BODY=Adds a small helper that runs a user-supplied command through the shell so callers can shell out from a single place.
DRAFT=false
