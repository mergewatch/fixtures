BRANCH=fixture/06-docs-only
TITLE=Request review on docs update
TAGS=config,skip,triggers,correctness
MODE=pr
BODY=Requests an on-demand MergeWatch review on the open docs PR with an @mergewatch mention.
DRAFT=false
REUSES=06-docs-only
SKIP_APPLY=true
# Manual: reuses 06's PR; needs a human @mergewatch review comment. Unattended, 06 asserts the comment is absent and 08 that it is present — same PR, opposite expectations.
MANUAL_ONLY=true
